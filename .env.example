# Gemini API Key (Required for extension build)
VITE_GEMINI_API_KEY="AIza..."

# Note: For Chrome Extensions, use the VITE_ prefix to ensure 
# the key is exposed to the browser build.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"
